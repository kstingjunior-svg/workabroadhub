node_modules/

# 2026-09: dist/ is built on Render during deploy (render.yaml runs
# `npm ci && npm run build`). Previously committed as a workaround
# for an earlier hosting setup; no longer needed.
dist/
.local/
.cache/
uploads/

.env
.env.*

*.log
*.db
*.sqlite

.DS_Store

server/public
vite.config.ts.*

*.tar.gz