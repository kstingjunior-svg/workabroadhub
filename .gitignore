node_modules/

# 2026-09: dist/ is now built on Render during deploy (render.yaml's
# buildCommand is `npm ci && npm run build`, which runs vite + tsc).
# Previously committed to git as a workaround for an earlier hosting
# setup; no longer needed. Gitignoring it removes the "rebuild dist,
# commit, push" ritual and the pre-push stale-dist check.
dist/
.local/
.cache/
uploads/

.env
.env.*

*.log
*.db
*.sqlite

.DS_Store

server/public
vite.config.ts.*

*.tar.gz